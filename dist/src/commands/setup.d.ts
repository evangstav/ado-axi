export declare const SETUP_HELP = "usage: ado-axi setup hooks\nInstalls the AXI session-start hook so agents get ado-axi's ambient context.";
export declare function setupCommand(args: string[]): Promise<string>;
